from setuptools import setup, find_packages

setup(
    name = 'wldvl',
    version = '1.0',
    packages = find_packages(),
    description = ("Python library to serial interface with Water Linked DVLs"),
    license = "MIT",
    keywords = "Water Linked WaterLinked waterlinked dvl serial a50"
)