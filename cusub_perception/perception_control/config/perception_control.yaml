perception_control:
  bb_controllers:

    # Success Thresholds
    succeeded_x_pixel_dist: 10
    succeeded_y_pixel_dist: 10

    # Controllers
    proportional:
      downcam:
        drive:
          max_setpoint: 1.0
          maxout_pixel_dist: 200
        strafe:
          max_setpoint: 1.0
          maxout_pixel_dist: 200